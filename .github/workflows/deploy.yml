name: Deploy FIAP Cloud Games (Local)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Executar deploy local Kubernetes
        shell: powershell
        run: |
          pwd
          ls
          .\deploy.ps1
